# Nixpacks fallback if Docker is disabled in Railway.
# Prefer Dockerfile + Caddy (see railway.toml).

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm run smoke"]

[start]
cmd = "npm run start"
